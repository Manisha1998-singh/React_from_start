import React from "react";

function Data(props) {
  return (
    <div>
      <div {...props}>
        We can also use the spread operator to prevent code repetition like in
        the above code
      </div>

      {/* We can also use the spread operator to prevent code repetition like in the above code */}
    </div>
  );
}

export default Data;
